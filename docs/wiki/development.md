# Development

Development notes and checklist.

1. Work in `scripts/` for the shell implementation.
2. Add tests under `tests/` and a CI workflow.
3. Follow the coding style in `repos/jules_dev_standard/template`.
