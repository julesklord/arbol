# Contributing

1. Fork the repo and create a topic branch.
2. Run `shellcheck scripts/mini-fetch.sh` and `make build` before opening a PR.
3. Include tests for new behaviors under `tests/` (bash bats or simple sh harness).
4. Keep commits small and focused; follow conventional commit messages.
