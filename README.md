# mini-fetch

Minimal fastfetch-style status tool. Contains a Go implementation (cmd/mini-fetch) and a tiny shell script (scripts/mini-fetch.sh).

Build: `cd cmd/mini-fetch && go build -o mini-fetch`
